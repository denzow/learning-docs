# 教材の作り方

このリポジトリに新しい学習教材を追加し、章を書き、音声を生成して公開するまでの手順をまとめる。
最初の教材「データモデリング」を作ったときの手順を、次の教材でそのまま繰り返せる形にしたものである。

## 教材の構成と規約

教材は `docs/` 直下のディレクトリ一つに収める。
ディレクトリ名は公開 URL のパスになるので、英小文字とハイフンで付ける（例 `data-modeling`）。

```
docs/<教材>/
  index.md                  教材のトップページ。目的、題材の方針、章ごとの目次の表
  01-<slug>.md              章の本文（NN は 2 桁の章番号）
  exercises/01-<slug>.md    章の演習問題
  audio-scripts/01-<slug>.md  章の読み上げ原稿
  audio/01-<slug>.mp3       読み上げ音声（scripts/generate-audio.sh が生成）
  audio/01-<slug>.timing.json  文ごとの開始時刻（同上）
```

本文、演習問題、読み上げ原稿、音声は同じ `NN-<slug>` のファイル名で対応づける。
サイトの nav（章ごとの入れ子）と章末の「演習と音声」の節は、`hooks/materials.py` がこの対応から組み立てるので、Markdown には書かない。
演習問題や音声がまだない章では、その項目が出ないだけで、ビルドは通る。

### 章の本文

章は「概念の解説」「具体例」「参考文献」の三部構成にする。
冒頭に H1 で「第N章 タイトル」を置き、続けて章の狙いと前章とのつながりを導入として書く。
H1 はそのまま nav のラベルになる。

具体例では、教材を通して一つの題材を使い、章同士が相互参照できるようにする。
データモデリングの教材では書籍オンラインストアを題材にし、ソースシステム（販売、倉庫、POS、電子書籍）を章が進むにつれて増やした。
総合演習のような通し設計の章だけは別の題材でよい。
例は特定製品に依存させず、一般的な SQL で書く。

ER 図は mermaid のコードブロックで書ける。
公開サイトでは図として描画されるが、ローカルの確認ではネットワークがないと描画されない。

### 図とイラスト

図は惜しまず入れる。
文章で位置関係や手順を説明するより図のほうが早く伝わる箇所は、章を書く段階で図にする。
図にする目安は次のとおりである。

- 空間の関係（機材の配置、断面、部品の位置と名称）
- 比較（条件を変えたときの見え方や結果を並べる）
- 手順や依存の流れ（段階ごとの状態、切り分けの順序、時間割）
- 数量の関係（段や距離のように、目盛りで見せると分かるもの）

1 章に 3〜5 枚を目安にし、概念の解説にも具体例にも置く。
図の種類ごとに使う道具を分ける。

- 流れ図や ER 図のように構造を示す図は mermaid で書く。
- 撮影の配置図のように、教材の中で同じ形の図を何枚も描くものは、仕様から生成するスクリプトを用意する（後述の `scripts/render-diagrams.py` がその例）。
- 機材や概念のイラストは SVG を手で書く。写実的な絵や製品写真の再現ではなく、位置関係と名称が分かる模式図でよい。

図は SVG で `docs/<教材>/img/` に置き、`figure` 要素でキャプションを付けて本文に載せる。
`attr_list` と `md_in_html` を有効にしてあるので、次の書き方がそのまま `<figure>` になる。
図番号は「図 章-連番」で、本文から「図 6-1」のように参照する。

```
<figure markdown="span">
![一灯と白レフ板の配置](img/06-one-light.svg)
<figcaption>図 6-1 一灯と白レフ板の配置（上から見た図）</figcaption>
</figure>
```

上から見たライティングの配置図は、JSON の仕様から `scripts/render-diagrams.py` が描く。
仕様は `diagrams/<教材>/<name>.json` に置き、座標はメートルで書く（原点は背景の中央、y はカメラ側が正）。
被写体、カメラ、ライト（ソフトボックス、アンブレラ、リフレクター、グリッド）、レフ板、寸法、角度、注記を描け、要素の書式はスクリプト冒頭の docstring にある。

```bash
scripts/render-diagrams.py <教材>            # diagrams/<教材>/*.json をすべて描く
scripts/render-diagrams.py <教材> 06-one-light  # 名前を指定して描く
```

機材や概念のイラストは SVG を手で書く（`fig-NN-<内容>.svg`）。
生成スクリプトと見た目を揃えるため、白地の角丸の枠、同じフォント指定、同じ配色（線 #333333、光 #f2a900、注記 #2f6db5、距離 #b3541e）を使う。
実在の製品は部品の位置関係が分かる模式図にとどめ、根拠のない細部やロゴを描かない。
SVG は白地で描いてあるので、ダークモードでも `docs/css/figures.css` により白いカードとして表示される。

出来上がりは PNG に描画して目で確かめる。

```bash
inkscape docs/<教材>/img/<name>.svg -o /tmp/<name>.png -w 900
```

### 演習問題

演習問題は四択で、章ごとに 5 問を目安にする。
ページの書式は `docs/js/quiz.js` が描画後の DOM から読み取るため、次の構造を崩さない。

- 「## 問題」と「## 解答と解説」の二つの節に分ける。
- 設問の見出しは「### 問N」で、問題と解答の節で同じ見出しを使う。
- 選択肢は「- A. 」から「- D. 」の箇条書きにする。
- 解答は「**解答**：C」のように書き、その後に解説の段落を続ける。

書式が読み取れないページは解答 UI に変換されず、素の Markdown として表示される。
総合演習のように章全体が演習の章では、演習問題を作らなくてよい。

### 読み上げ原稿

読み上げ原稿は、本文をそのまま読むのではなく、聴いて理解できるように書き直した話し言葉の文章である。
コードブロックや表は、その要点を言葉で説明する。
1 章あたり 4,300〜6,000 字が目安で、音声にすると 13〜20 分になる。

原稿の段落は地の文だけで書く。
`docs/js/audio-highlight.js` は「テキストだけを含む段落」を本文と見なして文ごとにハイライトするため、リンクや強調、コードブロック、表は入れない。
先頭の audio 要素と「対象章：」の行は雛形に含まれており、音声の生成時には読み上げ対象から除かれる。

edge-tts の字幕は原稿と一字一句ほぼ一致するが、中黒（・）が読点に置き換わることがある。
`scripts/srt-to-timing.py` が原稿側の表記に揃えるので、原稿の側で中黒を避ける必要はない。
また、edge-tts は長い原稿を内部で分割して合成するため、分割点にかかった一文が読み上げられているのに字幕から抜けることがある。
同じスクリプトが、字幕にない文を直前の文の終了時刻から始まる文として補うので、音声を作り直す必要はない。

## 手順

### 1. 計画を issue に書く

親 issue に教材の目的と章立てを書き、章ごとに子 issue を作る。
データモデリングの教材では、親 issue #1 の下に第 1 章から第 11 章までの子 issue #2〜#12 を作り、章ごとに 1 コミットで執筆して子 issue を閉じた。
演習問題（#14）や音声（#15）のように、章をまたぐ作業は別の issue にする。

### 2. 教材のディレクトリを作る

```bash
scripts/new-material.sh <ディレクトリ名> "<教材のタイトル>" "<タブのラベル>"
```

このスクリプトは `docs/<教材>/` と `index.md` の雛形を作り、`mkdocs.yml` の nav と `docs/index.md` の教材一覧に 1 行ずつ登録する。
登録された行の説明文と章数は手で書き換える。
README の教材一覧はスクリプトが登録しないので、手で 1 行足す。

### 3. 章を書く

章ごとに雛形を作り、本文から書く。

```bash
scripts/new-chapter.sh <教材> 01 <slug> "<章のタイトル>"
```

本文、演習問題、読み上げ原稿の三つの雛形ができる。
本文だけ先に作りたいときは、末尾に `chapter` を付ける（`exercise`、`audio-script` も同様）。
教材の `index.md` の目次の表に、章の行を足す。

原稿は `japanese-tech-writing` スキルの文章規範（一文一行、脚注、演出の抑制など）に従って書く。
Claude Code に執筆を頼むときは、規約の置き場所を指示に含める。

```
docs/<教材>/03-<slug>.md を執筆して。
教材の構成と書式は AUTHORING.md、文章規範は japanese-tech-writing スキルに従う。
題材と前章までの内容は docs/<教材>/index.md と 01, 02 章を参照する。
```

### 4. 音声を生成する

読み上げ原稿を書いたら、音声とタイミングを生成する。
edge-tts が必要で、合成にはネットワーク接続を使う。

```bash
pip install edge-tts
scripts/generate-audio.sh <教材>          # 全章
scripts/generate-audio.sh <教材> 03 04    # 第3章と第4章だけ
```

音声（`ja-JP-NanamiNeural`、標準話速）は環境変数 `VOICE` と `RATE` で変えられる。
mp3 は 1 章 5〜7MB で、リポジトリにコミットする。

原稿の表記を直しただけで音声を作り直したくないときは、`--timings-only` でタイミングだけを更新する。
同じ原稿を同じ音声設定で合成すれば再生時間は変わらないので、コミット済みの mp3 に対して字幕の時刻をそのまま使える。
原稿の文章そのものを変えたときは、mp3 ごと作り直す。

### 5. 確認する

ビルドは strict で通す。
リンク切れや nav の不整合は警告ではなくエラーになる。

```bash
mkdocs build --strict
```

演習の解答 UI と音声の再生は、ビルド結果をブラウザで開いて確認する。
`mkdocs serve` や Python の簡易サーバは HTTP の Range 要求に対応しておらず、mp3 のシークが先頭に戻ってしまう。
音声を確認するときは Range 対応のサーバで `site/` を配信する。

```bash
mkdocs build
npx http-server site -p 8000
```

### 6. 公開する

develop に push すると、GitHub Actions が main と develop の両方をビルドして GitHub Pages へデプロイする。
develop の内容は `/develop/` 配下でプレビューできる。
確認できたら develop から main への PR を作ってマージする。

## サイトの仕組みと変更時の注意

教材をまたいで共通の仕組みは次のファイルにある。

- `hooks/materials.py`：nav の教材行を章ごとの入れ子に展開し、章末に演習と音声への導線を足す。
- `hooks/redirects.py`：`mkdocs.yml` の `extra.redirects` に書いた旧 URL に転送ページを置く。
- `scripts/render-diagrams.py`：`diagrams/<教材>/*.json` からライティングの配置図の SVG を描く。
- `docs/css/figures.css`：本文の `figure` と `figcaption` の見た目。
- `docs/js/quiz.js`：演習ページ（URL に `/exercises/` を含む）を解答 UI に変換する。
- `docs/js/audio-progress.js`：読み上げ原稿ページ（URL に `/audio-scripts/` を含む）でプレイヤーを画面上部に固定し、再生位置を localStorage に保存する。保存キーは「教材/章」で、別の教材の同名の章と衝突しない。
- `docs/js/audio-highlight.js`：`timing.json` と原稿の段落を突き合わせ、再生中の一文をハイライトして追従する。
- `.github/workflows/deploy-pages.yml`：main と develop の二重ビルドとデプロイ。

これらを変えるときの注意点を挙げる。

- `exercises` と `audio-scripts` のディレクトリ名は、JS が URL で判定に使う。名前を変えるなら JS も変える。
- 見出しのテキストで判定する JS は、toc の permalink（¶ のアンカー）を除いてから比較する。`quiz.js` の `headingText` がその例である。
- Material はヘッダの高さを CSS 変数で公開していない。固定プレイヤーの位置は JS が実測して渡している。`.md-top`（ページトップへ戻るボタン）はインラインで位置が書かれるため、動かすには `!important` が要る。
- 音声の再生位置を復元するシークでも `timeupdate` が発火する。保存は再生中に限定しないと、位置 0 で上書きされる。
- Material は mermaid の図を closed な shadow root に描画する。描画の確認は要素の高さかスクリーンショットで行う。
- `navigation.indexes` が効くのは、セクションの先頭が `index.md` のときだけである。教材のトップページの名前は変えない。
- nav にラベルを書くと、そのラベルがページの `<title>` になる。「演習問題」のように章番号のないラベルにすると全章の `<title>` が同じになるので、hook は「第N章 演習問題」の形で付けている。
